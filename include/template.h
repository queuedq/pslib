#include <bits/stdc++.h>
#define endl "\n"
#define sz(x) ((int)x.size())
#define all(x) (x).begin(), (x).end()
using namespace std;
using lld = long long;
using pii = pair<int, int>;
using pll = pair<lld, lld>;
